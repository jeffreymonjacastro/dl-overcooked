# added_code

Codigo y configs generados durante la iteracion A/A2, retirados del area activa para dejar el repo base limpio.

Incluye:

- `policies/template.py`: version modificada que carga el router A2.2.
- `models/`, `training/`, `scripts/`, `tests/`: entrenamiento, diagnosticos y pruebas creadas para A/A2.
- `configs/evaluate_final.yaml`: config minima de smoke test final.
- `.gitignore`: archivo generado durante la iteracion.

La raiz activa conserva solo el reporte/pesos finales y el codigo original del repo.
